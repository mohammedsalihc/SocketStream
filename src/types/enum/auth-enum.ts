export enum ERole{
    ADMIN = "admin",
    USER = 'user'
}

export enum ELoginProvider{
    GOOGLE = "google",
    EMAIL = "email"
}

export enum ESocketIOEvent{
    CONNECTION = 'connection',
    DISCONNECT = 'disconnect',
    MESSAGE = 'message',
}